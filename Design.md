# Design System

## 1. 목적

이 문서는 제공된 HTML 화면을 기준으로 프로젝트 전체의 시각 언어와 UI 동작을 일관되게 적용하기 위한 단일 기준 문서다. 새로운 화면을 만들거나 기존 화면을 수정할 때 아래 토큰과 컴포넌트 규칙을 우선 사용한다.

## 2. 디자인 방향

- **명확함**: 정보 구조와 주요 행동이 한눈에 이해되어야 한다.
- **일관성**: 같은 의미의 요소는 모든 화면에서 같은 모양과 동작을 갖는다.
- **밀도 조절**: 업무 화면은 효율적인 정보 밀도를 유지하되, 그룹과 섹션 사이에는 충분한 여백을 둔다.
- **상태 중심**: 기본, 호버, 포커스, 비활성, 성공, 경고, 오류 상태를 누락하지 않는다.
- **접근성 우선**: 키보드 조작, 충분한 대비, 명확한 포커스 표시를 기본값으로 한다.

## 3. 디자인 토큰

### 3.1 색상

색상은 직접 입력하지 말고 의미 기반 토큰을 사용한다.

| 토큰 | 용도 | 기본값 |
|---|---|---|
| `--color-bg` | 전체 페이지 배경 | `#F7F8FA` |
| `--color-surface` | 카드·패널·입력 영역 | `#FFFFFF` |
| `--color-surface-muted` | 보조 배경·테이블 헤더 | `#F1F3F5` |
| `--color-text` | 본문 텍스트 | `#17202A` |
| `--color-text-muted` | 설명·보조 텍스트 | `#667085` |
| `--color-border` | 기본 경계선 | `#D0D5DD` |
| `--color-primary` | 주요 행동·활성 상태 | `#2563EB` |
| `--color-primary-hover` | 주요 행동 호버 | `#1D4ED8` |
| `--color-success` | 성공·정상 | `#16A34A` |
| `--color-warning` | 주의·대기 | `#D97706` |
| `--color-danger` | 오류·삭제·위험 | `#DC2626` |
| `--color-info` | 안내·정보 | `#0891B2` |

브랜드 컬러가 별도로 정의되어 있다면 위 의미 토큰의 값만 교체하고 컴포넌트 코드는 수정하지 않는다.

### 3.2 타이포그래피

- 기본 폰트: `Inter`, `Pretendard`, system sans-serif 순서
- 본문: `14px / 1.5`, `font-weight: 400`
- 보조 텍스트: `13px / 1.45`
- 페이지 제목: `28px / 1.25`, `font-weight: 700`
- 섹션 제목: `20px / 1.3`, `font-weight: 700`
- 컴포넌트 제목: `16px / 1.4`, `font-weight: 600`
- 버튼·라벨: `14px / 1.35`, `font-weight: 600`

텍스트는 색상만으로 의미를 전달하지 않는다. 오류, 성공, 상태 정보에는 아이콘 또는 명시적인 텍스트를 함께 사용한다.

### 3.3 간격

기본 단위는 `4px`다.

`4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64px`

- 입력 내부 여백: `12px 14px`
- 컴포넌트 사이 기본 간격: `16px`
- 카드 내부 여백: `24px`
- 섹션 사이 간격: `32px`
- 페이지 좌우 여백: 데스크톱 `32px`, 모바일 `16px`

### 3.4 모서리·그림자

- 작은 컨트롤: `6px`
- 입력·버튼: `8px`
- 카드·패널: `12px`
- 큰 모달·컨테이너: `16px`
- 기본 그림자: `0 1px 2px rgba(16, 24, 40, .06)`
- 부유 요소 그림자: `0 8px 24px rgba(16, 24, 40, .12)`

## 4. 레이아웃

화면은 다음 계층으로 구성한다.

1. **App shell**: 전역 내비게이션과 콘텐츠 영역
2. **Page header**: 페이지 제목, 설명, 주요 행동
3. **Toolbar**: 검색, 필터, 정렬, 보기 방식
4. **Content section**: 카드, 테이블, 폼, 차트
5. **Feedback**: 토스트, 인라인 메시지, 빈 상태, 오류 상태

콘텐츠 최대 너비는 `1440px`로 제한하고 가운데 정렬한다. 중요한 업무 데이터는 한 화면에서 우선순위가 분명하도록 배치한다.

## 5. 컴포넌트 규칙

### 버튼

- Primary: 화면의 가장 중요한 행동 한 개에 사용
- Secondary: 보조 행동
- Tertiary/Ghost: 낮은 우선순위 행동
- Danger: 삭제·취소 불가능한 행동

모든 버튼은 최소 높이 `40px`, 좌우 여백 `16px`, 아이콘과 텍스트 간격 `8px`를 사용한다. 아이콘만 있는 버튼에는 `aria-label`을 지정한다.

### 입력·검색

- 라벨은 입력 바깥에 항상 표시한다.
- 필수 항목은 별표만 사용하지 말고 오류 메시지로도 안내한다.
- 포커스 시 `--color-primary` 테두리와 외곽 링을 표시한다.
- 오류는 입력 아래에 원인과 해결 방법을 함께 표시한다.

### 카드·패널

카드 내부는 `24px` 여백, 제목·설명·본문의 계층 구조를 유지한다. 단순히 테두리만 추가해 정보를 그룹화하지 말고 제목과 간격으로도 그룹을 명확히 한다.

### 테이블

- 헤더는 본문보다 약간 어두운 보조 배경을 사용한다.
- 숫자 열은 우측 정렬하고 단위를 명시한다.
- 행 호버 상태를 제공한다.
- 모바일에서는 열 우선순위를 정해 축약하거나 카드 목록으로 전환한다.
- 데이터가 없을 때 빈 상태를 표시한다.

### 상태 배지

상태 배지는 짧은 텍스트와 의미에 맞는 색상을 함께 사용한다. 색상만으로 상태를 구분하지 않는다.

- 정상: success
- 진행 중/대기: info 또는 warning
- 실패/오류: danger
- 비활성: muted

### 모달·드로어

모달은 단일 목적의 짧은 작업에 사용하고, 복잡한 편집 작업은 드로어 또는 별도 페이지로 분리한다. 열릴 때 포커스를 내부로 이동하고 닫힌 뒤 원래 트리거로 되돌린다.

## 6. 반응형 기준

- `>= 1200px`: 다중 열 레이아웃과 전체 내비게이션
- `768px–1199px`: 사이드바 축소, 카드 열 수 감소
- `< 768px`: 단일 열, 터치 가능한 컨트롤 최소 `44px`

모바일에서는 장식보다 콘텐츠와 주요 행동을 우선한다. 가로 스크롤이 필요한 표는 사용자가 스크롤 가능하다는 표시를 제공한다.

## 7. 상태·상호작용

모든 상호작용 컴포넌트는 다음 상태를 구현한다.

`default → hover → focus-visible → active → disabled → loading → error/success`

로딩 중에는 버튼을 비활성화하고 중복 요청을 방지한다. 서버 작업이 오래 걸리면 스켈레톤 또는 진행 상태를 표시한다. 변경 사항이 저장되지 않은 상태에서 화면을 이탈할 경우 사용자에게 경고한다.

## 8. 접근성 기준

- 본문 대비는 WCAG AA 이상을 목표로 한다.
- 키보드만으로 모든 기능을 사용할 수 있어야 한다.
- `:focus`를 제거하지 말고 `:focus-visible` 스타일을 제공한다.
- 장식 이미지는 빈 `alt`, 정보 이미지는 의미 있는 `alt`를 사용한다.
- 입력 오류는 텍스트로 설명하고 관련 입력과 연결한다.
- 모션에는 `prefers-reduced-motion` 대응을 추가한다.

## 9. 구현 규칙

1. 새 색상·간격·폰트를 추가하기 전에 기존 토큰을 재사용한다.
2. 페이지별 임의 스타일보다 공통 컴포넌트 스타일을 먼저 수정한다.
3. HTML 구조와 시각 스타일을 분리한다.
4. 반복되는 스타일은 컴포넌트 또는 유틸리티로 추출한다.
5. 하드코딩된 색상과 간격을 의미 토큰으로 단계적으로 치환한다.
6. 모든 변경 후 데스크톱·모바일·키보드 상태를 확인한다.

## 10. 적용 체크리스트

- [ ] 전역 색상, 폰트, 간격 토큰 정의
- [ ] 페이지 배경과 콘텐츠 최대 너비 통일
- [ ] 헤더·내비게이션·페이지 제목 계층 통일
- [ ] 버튼, 입력, 카드, 테이블 공통 스타일 적용
- [ ] 성공·경고·오류·빈 상태 구현
- [ ] 모바일 레이아웃 확인
- [ ] 키보드 포커스와 색상 대비 확인
- [ ] 기존 화면의 임의 색상·간격 제거
- [ ] 주요 사용자 흐름 회귀 테스트

## 11. 변경 관리

디자인 변경은 먼저 이 문서의 토큰 또는 컴포넌트 규칙을 수정한 뒤 화면에 반영한다. 특정 화면에서만 필요한 예외는 이유와 적용 범위를 코드 주석 또는 변경 기록에 남긴다.
